{
  "name": "datastreams-backend",
  "version": "0.0.0",
  "description": "Simple Media Capture and Streams API extension to handle arbitrary data streams acquired through the browser.",
  "main": "./dist/backend/index.js",
  "module": "./dist/backend/index.js",
  "types": "./dist/backend/index.d.ts",
  "files": [
    "./dist"
  ],
  "scripts": {
    "start": "cd .. && yarn run backend",
    "build": "tsc"
  },
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "common": "*"
  },
  "dependencies": {}
}
