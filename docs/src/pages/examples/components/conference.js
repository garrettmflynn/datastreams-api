import React, { useEffect, useRef } from 'react';
import clsx from 'clsx';
import * as api from '../../../../../src/frontend';

export default function ConferenceExample({server}) {
  
    return (
        <div className="container">
          <h1 className="hero__title">Conference</h1>
          <p className="subtitle">To convert from HTML...</p>
        </div>
    );
  }
  